
// Re-export from hooks for backwards compatibility 
// until we can update all imports consistently
export { useAuth } from "@/hooks/useAuth";
export { AuthProvider } from "@/providers/AuthProvider";
